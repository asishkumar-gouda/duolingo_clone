apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "duolingo-clone.fullname" . }}-config
  labels:
    {{- include "duolingo-clone.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
